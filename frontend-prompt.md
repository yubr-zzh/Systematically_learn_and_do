# Systematically Learn and Do - 前端开发 Prompt

---

## 一、项目概述

**产品名称**：Systematically Learn and Do（系统化学习与执行工具）

**核心功能**：
1. **Learn（学习模式）**：帮助用户系统学习某个领域/概念，通过横纵分析法生成深度研究报告
2. **Project（项目模式）**：为项目/计划生成调研和规划，提供实施路线图

**目标用户**：需要系统性学习新领域、或开始新项目但缺乏结构化方法的职场人士、学生、研究者

---

## 二、设计风格规范

### 色彩体系
```
主色系 - 自然绿：
- 深海绿 #2D5016（强调色、按钮、导航高亮）
- 翠绿 #3D7A58（次级强调、进度条、图标）
- 薄荷绿 #E8F5E9（背景、卡片底色）
- 浅灰绿 #F1F8E9（次要背景）

中性色：
- 深灰 #2C3E50（文字主色）
- 中灰 #7F8C8D（辅助文字）
- 浅灰 #ECF0F1（边框、分隔线）
- 纯白 #FFFFFF（卡片背景）

辅助色：
- 暖米色 #FDF6E3（引用块、提示框背景）
- 珊瑚红 #E74C3C（错误、警告）
- 琥珀黄 #F39C12（注意、进行中）
- 天蓝 #3498DB（信息、链接）
```

### 字体规范
- **英文字体**：Inter（主）、JetBrains Mono（代码）
- **中文字体**：Noto Sans SC、PingFang SC
- **字号层级**：
  - H1: 32px / H2: 24px / H3: 20px / H4: 16px
  - 正文: 15px
  - 辅助: 13px
- **行高**：1.6（正文）、1.4（标题）

### 间距系统
- 基础单位：8px
- 模块间距：24px、32px、48px
- 卡片内边距：24px

### 圆角规范
- 小元素：8px
- 卡片：12px
- 模态框：16px

### 阴影层级
```
- 悬浮：0 4px 12px rgba(45, 80, 22, 0.1)
- 卡片：0 2px 8px rgba(0, 0, 0, 0.06)
- 模态框：0 8px 32px rgba(0, 0, 0, 0.12)
```

---

## 三、页面结构设计

### 全局布局
```
┌──────────────────────────────────────────────────────────────┐
│  HEADER [Logo | Learn | Project | Feedback | Settings | 用户] │
├─────────────┬────────────────────────────────────────────────┤
│             │                                                │
│  侧边导航    │              主内容区                          │
│  (可选)      │                                                │
│             │                                                │
│  📚 学习     │                                                │
│  📁 项目     │                                                │
│  💬 反馈     │                                                │
│  ⚙️ 设置     │                                                │
│             │                                                │
└─────────────┴────────────────────────────────────────────────┘
```

### Header 规范
- **高度**：64px
- **背景**：白色，底部阴影
- **Logo**：左侧，绿色渐变文字 "S²" 或 "系统学习与执行"
- **导航**：中间，四个 Tab（Learn / Project / Feedback / Settings）
- **用户区**：右侧，头像 + 下拉菜单
- **主题**： 右上角，可实现深/浅变换，响应式

---

## 四、各功能页详细设计

### 4.1 Learn 页面

#### 输入阶段
- **主题输入框**：大尺寸输入框，提示文字"输入你想学习的主题，如：React 框架、人工智能..."
- **快捷标签**：热门标签推荐（AI、编程、设计、商业...）
- **开始按钮**：绿色渐变，带研究图标

#### 报告生成阶段（三阶段进度）
```
┌─────────────────────────────────────────────────────┐
│  [🔄 阶段1: 横纵分析 进行中]  [⏳ 阶段2: 深度调研]  [⏳ 阶段3: 规划建议] │
│  ████████████░░░░░░░░░░ 45%                         │
└─────────────────────────────────────────────────────┘
```
- 每个阶段显示：状态图标、阶段名称、进度百分比
- 点击已完成阶段可回看

#### 报告展示区
- **左侧目录**：报告大纲（可折叠）
- **右侧内容**：markdown 渲染的报告
- **功能按钮**：收藏 ⭐、导出 PDF 📄、分享 🔗、历史记录 📜
- **阅读体验**：深色/浅色切换、字体大小调整

#### 历史学习列表
- 卡片展示：标题、创建时间、主题类型、字数统计
- 支持搜索、筛选、排序

---

### 4.2 Project 页面

#### 项目列表（卡片网格）
```
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ 项目封面图    │ │ 项目封面图    │ │ 项目封面图    │
│              │ │              │ │              │
│ AI 聊天机器人  │ │ 个人知识库    │ │ 电商小程序   │
│              │ │              │ │              │
│ 进行中 65%    │ │ 已完成 100%   │ │ 规划中 20%   │
│ 创建于 2024  │ │ 创建于 2024  │ │ 创建于 2024  │
│ [打开] [删除]│ │ [打开] [归档] │ │ [打开] [编辑]│
└──────────────┘ └──────────────┘ └──────────────┘
[+ 新建项目]
```

#### 新建项目表单
- 项目名称（必填）
- 项目描述/目标（必填）
- 预计周期
- 开始日期
- 项目类型标签（产品、技术、个人成长...）
- 参考链接（可选）

#### 项目详情（与 Learn 类似）
- 三阶段进度条
- 报告展示
- **任务追踪区**：可添加 TODO 项，标记完成状态
- **进度统计**：甘特图或时间线视图

---

### 4.3 Feedback 页面

#### 反馈列表
- 按报告分类展示
- 每条反馈显示：评分（星级）、评论内容、时间

#### 提交反馈表单
- 当前报告关联（自动）
- 评分：1-5 星
- 满意点：文本输入
- 改进建议：文本输入
- 附加评论：可选

---

### 4.4 Settings 页面

#### 用户配置
- 用户名、头像上传
- 主题偏好（明/暗/跟随系统）
- 字体大小滑块

#### 数据管理
- 导出数据（JSON/ZIP 包）
- 导入数据
- 清除历史记录
- 同步状态

#### Skill 配置
- 查看/编辑横纵分析法参数
- 深度调研参数设置
- 规划模板选择

---

## 五、交互细节规范

### 过渡动画
- 页面切换：淡入淡出（200ms）
- 卡片悬浮：上浮 4px + 阴影加深（200ms）
- 按钮点击：缩放 0.98（100ms）
- 进度条：平滑填充（300ms ease-in-out）

### 加载状态
- **骨架屏**：卡片、列表区域使用
- **进度指示**：报告生成时使用圆形进度条 + 文字说明
- **空状态**：插画 + 提示文字 + 引导操作

### 表单交互
- 实时验证（绿/红边框）
- 错误提示：红色文字，输入框下方显示
- 成功提示：绿色 Toast，右上角弹出

### 报告阅读
- **目录导航**：点击跳转对应章节
- **滚动高亮**：当前阅读章节在目录中高亮
- **章节折叠**：长报告支持分段折叠
- **引用标注**：参考文献链接可点击跳转

---

## 六、响应式设计

| 断点 | 设备 | 布局调整 |
|------|------|----------|
| ≥ 1200px | 桌面端 | 标准双栏布局 |
| 900-1199px | 平板横屏 | 侧边导航可收起 |
| 600-899px | 平板竖屏 | 单栏，顶部导航 |
| < 600px | 手机 | 底部 Tab 导航 |

### 移动端适配要点
- 侧边导航 → 底部 Tab Bar（Learn / Project / More）
- 报告目录 → 顶部折叠面板
- 卡片网格 → 单列堆叠
- 字体适当放大（正文 16px）

---

## 七、数据接口规范

### 后端 API 设计（供前端调用）

```typescript
// 学习报告
POST /api/learn/generate     // 生成学习报告
GET  /api/learn/list         // 获取学习列表
GET  /api/learn/:id          // 获取单份报告
PUT  /api/learn/:id/archive  // 归档
DELETE /api/learn/:id        // 删除

// 项目
POST /api/projects/create    // 创建项目
GET  /api/projects/list      // 项目列表
GET  /api/projects/:id       // 项目详情
PUT  /api/projects/:id/tasks // 更新任务
POST /api/projects/:id/report // 生成项目报告

// 反馈
POST /api/feedback           // 提交反馈
GET  /api/feedback/:reportId // 获取报告反馈

// 设置
GET  /api/settings           // 获取设置
PUT  /api/settings           // 更新设置
POST /api/data/export        // 导出数据
POST /api/data/import        // 导入数据
```

### 数据模型

```typescript
// 学习报告
interface LearnReport {
  id: string;
  title: string;
  subject: string;
  status: 'generating' | 'completed' | 'archived';
  progress: number; // 0-100
  createdAt: string;
  updatedAt: string;
  reportContent: string; // markdown
  versions: string[]; // 历史版本
}

// 项目
interface Project {
  id: string;
  name: string;
  description: string;
  type: string;
  status: 'planning' | 'in_progress' | 'completed';
  progress: number;
  tasks: Task[];
  reportContent: string;
  createdAt: string;
  dueDate?: string;
}

// 反馈
interface Feedback {
  id: string;
  reportId: string;
  rating: number; // 1-5
  comment: string;
  likes: number;
  createdAt: string;
}
```

---

## 八、技术栈推荐

```
前端框架: React 18 或 Vue 3（任选其一）
状态管理: Zustand（React）/ Pinia（Vue）
样式方案: Tailwind CSS 或 CSS Modules
Markdown: react-markdown / marked + highlight.js
图表: Recharts / Chart.js（甘特图、进度图）
图标: Lucide React / Heroicons
动画: Framer Motion（React）/ VueUse（Vue）
HTTP: axios / fetch
存储: IndexedDB（本地持久化）+ localStorage（配置）
```

---

## 九、开发任务分解

### Phase 1: 基础框架
- [ ] 项目初始化（选择框架，配置 Tailwind）
- [ ] 路由配置（四个主要页面）
- [ ] Header 组件
- [ ] 全局样式与主题变量

### Phase 2: Learn 功能
- [ ] 学习输入表单
- [ ] 三阶段进度条组件
- [ ] Markdown 报告渲染器
- [ ] 学习历史列表

### Phase 3: Project 功能
- [ ] 项目列表卡片
- [ ] 新建项目表单
- [ ] 项目详情（含任务追踪）
- [ ] 甘特图/时间线视图

### Phase 4: Feedback & Settings
- [ ] 反馈提交与展示
- [ ] 设置页面（用户配置、数据管理）
- [ ] 主题切换

### Phase 5: 优化与适配
- [ ] 响应式适配（移动端）
- [ ] 加载状态与空状态
- [ ] 性能优化
- [ ] 测试与修复

---

## 十、注意事项

1. **报告渲染性能**：长报告（可能 10000+ 字）需考虑虚拟列表或分段加载
2. **数据持久化**：本地存储需注意容量，大文件建议压缩存储
3. **Markdown 安全**：对用户内容做好 XSS 过滤
4. **错误边界**：报告加载失败时的降级处理
5. **无障碍**：基本 ARIA 标签，键盘导航支持

---

## 十一、交付要求

- 完整的可运行前端项目
- 清晰的代码注释
- 必要的使用说明文档
- 响应式适配所有主流设备
